// SqrtTree
// replying to queries for a segment in O(1) time, and modifying one element in O(\sqrt N)
// Verified : <url>

// dependencies
// <#include "lib/hoge.hpp">
// <#include <bits/stdc++.h>>


/* SqrtTree */
// replying to queries for a segment in O(1) time, and modifying one element in O(\sqrt N)
//
// List of Member
// param:
// 1. <p>: <type>
// function:
// 1. <f>(<p>: <type>) -> <type>
template <class Monoid> class SqrtTree {
  public:

  private:
    
};
